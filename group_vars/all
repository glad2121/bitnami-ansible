---
# group_vars/all

remote_host: "{{ ansible_eth0.ipv4.address }}"
remote_user: "{{ ansible_env.SUDO_USER | default(ansible_user_id) }}"

yum_local_repo_url: file:///vagrant/repo
bitnami_downloads_dir: /vagrant/downloads
